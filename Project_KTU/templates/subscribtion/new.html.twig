{% extends 'base.html.twig' %}

{% block title %}New Subscribtion{% endblock %}

{% block body %}
    <h1>Subscribe to</h1>
    {% if form is defined%}
    {{ include('subscribtion/_form.html.twig') }}
    {% else %}
    No categories found
    {% endif %}

    <a class="btn btn-secondary" href="{{ path('subscribtion_index') }}">back to list</a>
{% endblock %}
